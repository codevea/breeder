{% for label, messages in app.flashes(['success', 'warning', 'info', 'error']) %}
    {% for message in messages %}
        <div class="{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}